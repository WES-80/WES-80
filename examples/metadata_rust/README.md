# Metadata Example (Rust)

Shows how metadata can be included in a WES game.

## Build

Run the following command to build the example:

```sh
cargo build --target wasm32-unknown-unknown --release
```
